# Force 'make' without target do nothing, so that the README.md does not
# become autogenerated during automated testing through ../Makefile.
all:

# Do nothing for clean
clean:

README.md: generate_readme.py generate_combined.awk
	./generate_readme.py > $@
